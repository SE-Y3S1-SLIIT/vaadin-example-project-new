package com.example.notes.components.gallery;

/**
 * Responsibilities:
 * - Reusable Vaadin uploader component handling drag/drop and previews.
 * - Emit events or call `ImageService` to save uploaded files.
 */
public class ImageUploaderComponent {
    // TODO: implement upload UI wrapper (Upload, callbacks, validation)
}
