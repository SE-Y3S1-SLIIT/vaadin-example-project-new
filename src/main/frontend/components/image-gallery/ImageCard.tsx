import React from 'react';

// Responsibilities:
// - Present a single image, metadata and actions (delete, download)

export default function ImageCard() {
  return (
    <div className="image-card">
      {/* TODO: thumbnail, title, actions */}
    </div>
  );
}
